import React from 'react';

class ReviewTitle extends React.Component {

    render() { 
        return ( <span>
                   {this.props.title}
                </span> 
        );
    }
}
 
export default ReviewTitle